# URL interna del backend PHP (solo servidor Nuxt, no expuesta al navegador)
API_INTERNAL_URL=http://localhost:8081

# Token REST — solo en servidor (no usar NUXT_PUBLIC_)
API_TOKEN=change-me-generate-random-string

# Puerto del dashboard
PORT=3000
HOST=0.0.0.0
